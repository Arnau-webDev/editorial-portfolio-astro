---
interface Props {
  number: string;
  title: string;
}

const { number, title } = Astro.props;
---

<div class="mb-8 md:mb-12">
  <p class="text-primary text-base tracking-widest uppercase mb-3">{number}</p>
  <h2 class="font-serif text-3xl md:text-4xl text-foreground">{title}</h2>
</div>
